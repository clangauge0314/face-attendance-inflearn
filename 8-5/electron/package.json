{
  "name": "electron-app",
  "version": "1.0.0",
  "main": "main.js",
  "scripts": {
    "start": "cd ../frontend && npm run build && cd ../electron && electron ."
  },
  "dependencies": {
    "electron": "^36.3.2"
  }
}
